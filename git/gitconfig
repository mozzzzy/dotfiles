# This is Git's per-user configuration file.
[user]
  # FIXME
  name =
  email =
[core]
  editor = vim
[push]
  default = simple
[alias]
  b = branch -a
  d = diff
  l = log --oneline --graph --all
  r = remote -v
  s = status
